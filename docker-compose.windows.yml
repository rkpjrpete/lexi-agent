#
# docker-compose.windows.yml — Windows Docker Desktop compatible for Lexi VPA
#
# Differences from docker-compose.yml:
#   - Removes `network_mode: host` (not supported on Docker Desktop for Windows)
#   - Uses explicit port mappings instead
#   - Uses Windows-style volume path for ~/.lexi
#
# Usage:
#   docker compose -f docker-compose.windows.yml up -d
#
services:
  gateway:
    build: .
    image: lexi-agent:latest
    container_name: lexi
    restart: unless-stopped
    volumes:
      - ${USERPROFILE}/.lexi:/opt/data
    environment:
      - LEXI_UID=10000
      - LEXI_GID=10000
      - HERMES_UID=10000
      - HERMES_GID=10000
    command: ["gateway", "run"]

  dashboard:
    image: lexi-agent:latest
    container_name: lexi-dashboard
    restart: unless-stopped
    depends_on:
      - gateway
    volumes:
      - ${USERPROFILE}/.lexi:/opt/data
    environment:
      - LEXI_UID=10000
      - LEXI_GID=10000
      - HERMES_UID=10000
      - HERMES_GID=10000
      - HERMES_DASHBOARD_HOST=0.0.0.0
    ports:
      - "127.0.0.1:9119:9119"
    command: ["dashboard", "--host", "0.0.0.0", "--port", "9119", "--no-open", "--insecure"]

